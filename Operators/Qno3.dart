/* 3. Create two boolean variables: isNepali = true, isCoder = false. Print results of AND, OR, and NOT operations. */
void main(){
 bool isNepali = true, isCoder = false;
 print("AND : ${isNepali && isCoder}");
  // ignore: dead_code
  print("OR : ${isNepali || isCoder}");
   print("NOT : ${!isNepali}");

}
