/* 4. Create a set of numbers {1, 2, 3, 4, 4, 5} and print it. What happens to the duplicate? */
void main(){
  // ignore: equal_elements_in_set
  Set<int> numbers ={1,2,3,4,4,5};
  print(numbers);
  //The duplicate valuse is not printed.
}